When you make changes to any of these javascript/typescript assets,
they have to be build (transpiled) to the `dist` folder, so that the
applications that use this package, can publish those generated assets.

See [DEV.md](src/DEV.md) for more details regarding the `build` command.
